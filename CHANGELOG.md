## Unreleased

- Expose the SDK pageview and visitor identifiers so host apps can correlate their own analytics with SDK pageviews. Native dependency pins must be bumped when the native SDKs containing this API are released.

## 1.8.0

- Bump native Ezoic Ads SDK pins to 1.8.0. `testMode` now also requests $0.00 Ezoic test-ad fill on no-demand auctions in development builds and simulators.
